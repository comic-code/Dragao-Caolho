import styled from 'styled-components';

export const SpellBodyWrapper = styled.div`
  padding-top: 0.5rem;
  max-width: 100%;
  display: flex;
  flex-direction: column;

  p {
    margin: 0.2rem 0;
  }

  .bold {
    font-weight: bold;
  }

  ul,
  li {
    margin: 0.5rem;
  }
`;
